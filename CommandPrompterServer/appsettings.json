{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*",
  "JwtToken": { // Note the secret key length should be a minimum of 128 bits i.e 16 bytes
    "SecretKey": "test_secret_key_test_secret_key_test_secret_key_test_secret_key_test_secret_key_test_secret_key", //Signing Credentials with secured alogrithm and key that signs the security token.
    "Issuer": "https://localhost:44341", //A valid issuer that will be used to check against the token's issuer
    "Audience": "https://localhost:44341", //A valid audience that will be used to check against the token's audience
    "TokenExpiry": "60" //Define the lifetime expiry of token once issued from current date and timestamp
  },
  "AuthLogin": {
    "Username": "testuser",
    "Password": "testuser"
  },
  "ConnectionString": "Source=192.168.8.29;User Id=sa;Password=111111;Initial Dialog=CommandPrompter;",
  "LogPosition": "./log.txt"
}
